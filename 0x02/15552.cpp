#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int TC, a, b;
	cin >> TC;
	for (int tc = 0; tc < TC; tc++) {
		cin >> a >> b;
		cout << a + b << '\n';
	}
}