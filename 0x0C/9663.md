# 개선점
- 퀸이 이동할 수 있는 대각선 위치
  - 왼쪽 위 -> 오른쪽 아래 대각선
	- x가 1 증가할 때, y도 1 증가한다.
	- 따라서 한 대각선 내의 칸들은 `x - y`가 같다.
  - 왼쪽 아래 -> 오른쪽 위 대각선
    - x가 1 감소할 때, y는 1 증가한다.
	- 따라서 한 대각선 내의 칸들은 `x + y`가 같다.
  - 위 두 성질을 이용해, 어떤 대각선 칸이 사용중인지 아닌지 쉽게 관리할 수 있다.
  - 배열로 관리하기 위해 `x - y`는 `x - y + N - 1`로 음수를 없앨 수 있다.