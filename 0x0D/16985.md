# 오답노트
- `bfs`에서 시작점이 0인 경우를 생각 못함.
- `if (cube[0][0][0] == 0 || cube[4][4][4] == 0) return -1;`

# 개선점
- 판 쌓는 순서 `next_permutation`으로 가능.
- `bfs`에서 도착점에 도달했을 때, 조기 종료 가능.
  - `if(nx == 4 && ny == 4 && nz == 4)`
- 회전된 판의 모양을 미리 계산해놓기. (적용됨)
- 판들이 회전된 방향을 결정하기 위해 진법 사용 가능.